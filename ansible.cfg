[defaults]
# Path to your inventory file
inventory = ./hosts.ini  

# Roles path (auto-detects roles from this directory)
roles_path = ./  

# Disable host key checking (for testing)
host_key_checking = False  

[privilege_escalation]
become = True
become_method = sudo