This shows issues faced with node and how to install node, NVM. 


https://nodesource.com/products/distributions


While using NVM, ensure to always run this command in a new shell to export nvm and CI

```
export NVM_DIR="$HOME/.nvm"
source "$NVM_DIR/nvm.sh"
```
